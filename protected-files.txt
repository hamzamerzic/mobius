# Protected files — read-only for the agent.
#
# Entries are ABSOLUTE paths. The entrypoint enforces root ownership
# and read-only modes after the chown sweep that hands /data/platform to the
# mobius user.
#
# --- Baked boot/runtime infrastructure ---
# The whole-repo model un-freezes the backend: the SERVED code is
# /data/platform/backend/app (mobius-owned, editable in place), so the old
# /app/app/* freeze protected the baked FALLBACK, not the live code — moot now.
# The externally launched recovery target starts through the baked entrypoint
# before /data is touched. Ordinary restart authorization remains baked too.
/app/scripts/entrypoint.sh
/app/scripts/boot_attempt_counter.py
/app/scripts/agent_sudo.sh
/app/runtime/restart_ledger.py
